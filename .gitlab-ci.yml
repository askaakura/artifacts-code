stages:
  - build
  - deploy

build:
  stage: build
  script:
    - apt-get update -y
    - apt-get install npm -y
    - npm install

deploy:
  stage: deploy
  script:
    - apt-get update -y
    - apt-get install nodejs -y
    - node index.js